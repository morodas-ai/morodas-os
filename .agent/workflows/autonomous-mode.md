---
description: 自律実行モード - 承認なしで即実行、結果だけ報告
---
// turbo-all

## ルール
- 計画書の承認は不要。方向性が明確なら即実行する。
- コマンド実行（npm install, prisma migrate等）は自動承認。
- 完成してから結果を報告する（途中経過の確認は不要）。
- サーバー再起動等のインフラ操作も自律で行う。

## 例外（必ず確認を取る）
- 💰 お金が絡む操作（有料パッケージ契約等）
- 🌐 本番環境へのデプロイ（Vercel / VPS）
- 🔑 外部API本番接続（Stripe等）
